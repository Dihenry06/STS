# Environment Variables for Security Token Service

# JWT Configuration
# Generate a secure secret with at least 32 characters
# You can use: openssl rand -base64 32
JWT_SECRET=your-super-secure-jwt-secret-key-here-minimum-32-characters
JWT_EXPIRATION=3600

# MongoDB Configuration (Production)
MONGODB_URI=mongodb://username:password@hostname:27017/database?authSource=admin
MONGODB_DATABASE=security-token-service

# Redis Configuration (Production)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=your-redis-password-here

# Rate Limiting Configuration
RATE_LIMIT_REQUESTS=30
RATE_LIMIT_ENABLED=true

# Application Environment
SPRING_PROFILES_ACTIVE=prod

# Optional: Logging Configuration
LOGGING_LEVEL_ROOT=INFO