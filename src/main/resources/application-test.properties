# Test Environment Configuration

# MongoDB Configuration for Tests
spring.data.mongodb.uri=mongodb://admin:password@localhost:27017/security-token-service-test?authSource=admin
spring.data.mongodb.database=security-token-service-test

# JWT Configuration for Tests
jwt.secret=testSecretKey123456789012345678901234567890
jwt.expiration=300

# Logging
logging.level.com.dhs.platform.security_token_service=INFO
logging.level.root=WARN

# Disable database initialization in tests
spring.datasource.initialization-mode=never

# Redis Configuration for Tests
spring.data.redis.host=localhost
spring.data.redis.port=6379
spring.data.redis.password=Teste@123

# Rate Limiting Configuration for Tests (disabled or high limit)
rate-limit.requests-per-minute=1000
rate-limit.enabled=false